import { User } from '../../../../types';
import { StyledContainer } from './styles';

type EditBarHeaderProps = { user: User };

const EditBarHeader: React.FC<EditBarHeaderProps> = ({ user }) => {
  return <StyledContainer>test</StyledContainer>;
};

export default EditBarHeader;
